#!/bin/bash
# ~/.bash_completion - user bash completions

# Use SSH completions for `sshpass', `sshrc', `ssh-guest' & `sshrc-guest'
complete -F _comp_cmd_ssh sshpass sshrc ssh-guest sshrc-guest
# Use SCP completions for `scppass' & `scp-guest`
complete -F _comp_cmd_scp -o nospace scppass scp-guest
